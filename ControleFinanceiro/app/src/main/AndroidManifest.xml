<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.ControleFinanceiro">

        <!-- Tela de Lançamento (Cadastro) -> tela principal (launcher) do app -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Tela de Extrato (lista de lançamentos) -->
        <activity
            android:name=".ExtratoActivity"
            android:exported="false"
            android:label="@string/titulo_extrato" />

    </application>

</manifest>
